// --- VARIÁVEIS DE CONEXÃO E URL ---
const urlParamsAdmin = new URLSearchParams(window.location.search);
const adminSalaId = urlParamsAdmin.get('idsala') || '000'; // Pega da URL ou assume 000

// Atualiza título da aba/navegador para facilitar identificação
document.title = `MESA ${adminSalaId} - BINGO`;

let socket = null;
let reconnectInterval = null;
let countdownInterval = null;
let houveGanhadorNaSessao = false;
const RECONNECT_DELAY = 5000;

// O WS_URL precisa passar o ID da sala para o servidor validar
// Se estiver rodando local, usa o host da janela.
const protocol = window.location.protocol === 'https:' ? 'wss:' : 'ws:';
const WS_URL = `${protocol}//${window.location.host}/ws?idsala=${adminSalaId}`; 
const API_BASE_URL = "";